# Command-deck shell — dark, to match the embedded dashboard.
[theme]
base = "dark"
primaryColor = "#49d6e6"
backgroundColor = "#05070e"
secondaryBackgroundColor = "#0a1020"
textColor = "#eaeefb"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
